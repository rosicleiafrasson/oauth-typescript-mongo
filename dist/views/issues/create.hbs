<div>
    <div >
        <div class="card">
            <div class="card-body">
                <p class="lead">Nova Issue</p>
                <form action="/issues/create" method="POST">
                    <div class="form-group">
                        <input type="text" name="title" placeholder="Título" required="true" class="form-control" autofocus>
                    </div>
                    <div class="form-group">
                        <textarea class="form-control" name="description" required="true" placeholder="Descreva seu problema" rows="3"></textarea>    
                    </div>
                    <div class="form-group">
                        <label>Atribuir a: </label>
                        <select name="userAssignId" class="custom-select mr-sm-2"> 
                            <option selected>Escolha</option>
                        {{#each usersFounded}}
                            <option value="{{id}}">{{email}}</option>                      
                        {{/each}}
                        </select>
                    </div>
                    <button type="submit" class="btn btn-success">Salvar</button>
                </form>
            </div>
        </div>
    </div>
</div>